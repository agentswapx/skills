# Skills 目录

这个目录是仓库内独立技能包的索引入口。

- [`atxswap/`](./atxswap/) —— ATX 在 BSC 链上 ATXSwap 智能体交易所的技能包，
  同一份 `SKILL.md` 同时兼容以 **Claude Code** 和 **OpenClaw** 为代表的客户端。

每个技能目录都会各自维护自己的 `SKILL.md`、包元数据、脚本和 README，
不再把这些说明混放在 `skills/` 根目录下。
