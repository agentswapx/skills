# Skills 目录

这个目录是仓库内独立技能包的索引入口。

- [`atxswap/`](./atxswap/) —— 统一的 ATX 交易技能，同一份 `SKILL.md` 同时
  兼容 **Claude / Cursor / Codex CLI**（skills.sh 运行时）与 **OpenClaw /
  ClawHub**。

每个技能目录都会各自维护自己的 `SKILL.md`、包元数据、脚本和 README，
不再把这些说明混放在 `skills/` 根目录下。
